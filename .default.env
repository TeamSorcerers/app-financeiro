###############################################################################
# Configurações do Banco de Dados
###############################################################################
# URL de conexão completa para o banco de dados MySQL.
# Formato: mysql://USUARIO:SENHA@HOST:PORTA/NOME_DO_BANCO
DATABASE_URL=""

###############################################################################
# Configurações de Autenticação
###############################################################################
# Chave secreta utilizada para assinatura e verificação de tokens JWT.
# Recomenda-se uma string longa, complexa e única.
AUTH_SECRET=""
AUTH_TRUST_HOST=true

###############################################################################
# Configurações do Servidor
###############################################################################
# Ambiente de execução da aplicação.
# Valores possíveis: "development" ou "production"
# Recomenda-se "production" para ambientes de produção.
APP_ENV="development"

# Porta na qual o servidor Node.js irá escutar.
# Exemplo: 3000, 4000, etc.
APP_PORT=3001

###############################################################################
# Configurações do Cliente
# Essas variáveis precisam ser definidas com o prefixo NEXT_PUBLIC_
# para serem acessíveis no lado do cliente.
###############################################################################
# URL base da API utilizada pelo cliente.
# Exemplo: "/api" para rotas relativas ao servidor.
# Isso permite que o cliente faça requisições para a API sem precisar especificar o domínio.
NEXT_PUBLIC_API_BASE_URL="/api"

# Subpasta onde o cliente está hospedado.
# Isso é útil se o cliente estiver em uma subpasta do domínio principal.
# Exemplo: "sitec" se o cliente estiver acessível em "https://example.com/sitec".
# Deixe vazio se o cliente estiver na raiz do domínio.
# Aviso: Isso afeta as URLS de requisições feitas pelo cliente. Exemplo: "/api" se o cliente estiver na raiz, ou "/sitec/api" se estiver em uma subpasta.
NEXT_PUBLIC_APP_SUBFOLDER=""